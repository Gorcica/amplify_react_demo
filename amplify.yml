version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ls
        - npm ci --force
    build:
      commands:
        - ls
        - npm run build:dev
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: node_modules/**/*