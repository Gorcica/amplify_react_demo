version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ls
        - npm ci --force
    build:
      commands:
        - ls
        - npm run build:dev
        - ls
  artifacts:
    baseDirectory: ./public
    files:
      - '**/*'
  cache:
    paths: node_modules/**/*